{
  "name": "noobs-server",
  "version": "1.0.0",
  "description": "Place to store all our memories!",
  "main": "index.js",
  "scripts": {
    "start": "env-cmd node index.js -R .env",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ISE NOOBS",
  "license": "ISC",
  "dependencies": {
    "env-cmd": "^10.0.1",
    "express": "^4.17.3"
  }
}
